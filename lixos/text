
/* Estilo do botão para abrir o modal */
button.buttonOpen {
    padding: 10px 20px;
    font-size: 16px;
    background-color: #6200ea;
    color: #fff;
    border: none;
    border-radius: 5px;
    cursor: pointer;
}

/* Estilos para o modal */
dialog {
    border: none;
    border-radius: 10px;
    width: 800px; /* Aumentando a largura do modal para evitar rolagem */
    padding: 0;
    backdrop-filter: blur(4px);
    max-width: 90vw; /* Responsivo para telas menores */
}

.ModalContainer {
    background-color: #4c247d; /* Cor de fundo apenas no modal */
    color: #fff;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    display: flex;
    flex-direction: column;
}

header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
}

button.buttonClose {
    background: red;
    border: none;
    color: #fff;
    font-size: 20px;
    cursor: pointer;
    padding: 5px 10px;
    border-radius: 5px;
}

button.buttonConcluir {
    background-color: #e040fb;
    border: none;
    color: #fff;
    padding: 8px 20px;
    border-radius: 5px;
    cursor: pointer;
    font-size: 16px;
}

/* Layout do formulário */
form {
    display: flex;
    gap: 20px;
}

/* Estilo para o lado esquerdo e direito */
.LadoEsquerdo, .LadoDireito {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 15px;
}

/* Estilo dos campos individuais */
.FormProduto, .FormQuantidade, .FormPreco, .FormTipo, .FormImagem {
    display: flex;
    flex-direction: column;
}

label {
    font-size: 14px;
    color: #ddd;
}

input[type="text"], input[type="number"] {
    width: 100%;
    padding: 10px;
    border: none;
    border-radius: 5px;
    font-size: 14px;
    color: #333;
}

input[type="text"]::placeholder,
input[type="number"]::placeholder {
    color: #bbb;
}

/* Estilo específico para o campo de upload */
.FormImagem {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: 100%;
    height: 200px; /* Definindo altura específica para dar o efeito de "quadrado" */
    background-color: #5a2b89; /* Cor de fundo similar ao modal */
    border: 2px dashed #ddd;
    border-radius: 8px;
    color: #ddd;
    text-align: center;
    position: relative;
}

.FormImagem label {
    cursor: pointer;
    font-size: 14px;
    color: #ddd;
}

.FormImagem label::after {
    content: "Clique para escolher uma imagem ou arraste para enviar";
    display: block;
    margin-top: 10px;
    font-size: 12px;
    color: #bbb;
}

.FormImagem input[type="file"] {
    display: none;
}


.FormTipo {
    display: flex;
    align-items: center;
    gap: 10px;
}

.FormTipo label {
    color: #ddd;
    font-size: 14px;
    cursor: pointer;
}

input[type="checkbox"] {
    margin-right: 5px;
}
